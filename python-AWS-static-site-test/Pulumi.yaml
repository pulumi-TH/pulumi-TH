name: python-AWS-static-site-test
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a static website on AWS
config:
  pulumi:tags:
    value:
      pulumi:template: static-website-aws-python
